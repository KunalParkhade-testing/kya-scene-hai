import Link from 'next/link';import SiteHeader from '@/components/SiteHeader';import SiteFooter from '@/components/SiteFooter';import {stories} from '@/lib/content'
const topics=['Systems','Economics','Culture','Science','Infrastructure','What If?']
export default function Topics(){return <div className="site"><SiteHeader/><main className="archive"><div className="kicker">Browse by topic</div><h1>GO WHERE<br/>CURIOSITY GOES.</h1><div className="topics">{topics.map(t=><span className="topic" key={t}>{t}</span>)}</div><div className="grid topic-grid">{stories.map(s=><Link className="card" href={`/articles/${s.slug}`} key={s.slug}><div className={`cover ${s.color}`}><span className="tag">{s.category}</span><strong>{s.hero}</strong></div><div className="cardbody"><h3>{s.title}</h3><p>{s.dek}</p></div></Link>)}</div></main><SiteFooter/></div>}
