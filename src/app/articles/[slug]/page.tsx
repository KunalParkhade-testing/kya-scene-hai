import {notFound} from 'next/navigation';import Link from 'next/link';import SiteHeader from '@/components/SiteHeader';import SiteFooter from '@/components/SiteFooter';import {stories} from '@/lib/content'
export default async function Article({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=stories.find(x=>x.slug===slug);if(!s)notFound();return <div className="site"><SiteHeader/><main className="article"><Link href="/articles" className="back">← Back to archive</Link><div className="kicker article-kicker">{s.category}</div><h1>{s.title}</h1><p className="lede">{s.dek}</p><div className="meta">{s.read} · Kya Scene Hai?</div><div className={`articlehero ${s.color}`}><h2>{s.hero}</h2></div><article className="prose">{s.content.map((p,i)=><p key={i}>{p}</p>)}<blockquote>“Bhai… scene kya hai?”</blockquote><h2>So, what should you remember?</h2><p>The goal of Kya Scene Hai? is not to overload you with information. It is to leave you with a mental model — something you can use the next time this story comes up in real life.</p></article></main><SiteFooter/></div>}
