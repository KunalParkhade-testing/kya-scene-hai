import Link from 'next/link';import SiteHeader from '@/components/SiteHeader';import SiteFooter from '@/components/SiteFooter';import {stories} from '@/lib/content'
export default function Articles(){return <div className="site"><SiteHeader/><main className="archive"><div className="kicker">The archive</div><h1>ALL THE<br/>SCENES.</h1><div className="searchbar"><input placeholder="Search stories, topics, questions…"/><button>Search</button></div><div className="grid">{stories.map(s=><Link className="card" href={`/articles/${s.slug}`} key={s.slug}><div className={`cover ${s.color}`}><span className="tag">{s.category}</span><strong>{s.hero}</strong></div><div className="cardbody"><h3>{s.title}</h3><p>{s.dek}</p><div className="read">Open story →</div></div></Link>)}</div></main><SiteFooter/></div>}
